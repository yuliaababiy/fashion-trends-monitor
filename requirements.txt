# Minimal requirements for Streamlit Cloud (UI only, no training)
# The dashboard reads pre-computed parquet/csv files from the repo.
streamlit>=1.32
pandas>=2.1,<3
numpy>=1.26,<2
pyarrow>=14.0
plotly>=5.18
